Sorted array first as we dynamically loop over the data structure.    
When the new data come in, the information we need is only the index of the previously sorted array.    
Binary search will be the answer.   
